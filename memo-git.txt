/*** first time ***/

// my acount
git config --global user.email "kota.lalala.2021@gmail.com"
git config --global user.name "kota-2021"

// used tool ( code is 'vscode' )
git config --global core.editor 'code --wait'
git config --global merge.tool 'code --wait "$MERGED"'

// push
git config --global push.default simple

git init
git add golang-golearn-first.go
git commit -m "first golang program create"

git remote add origin https://github.com/Kota-2021/golang-golearn-first.git
git branch -M main
git push -u origin main

/*
git status
git log
git branch
git update-git-for-windows
git --version
*/

/*** second time ***/

git pull
git branch baby
git checkout baby

/*
git branch
*/
